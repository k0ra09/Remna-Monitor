version: "3.9"

services:
  agent:
    build: ./agent
    container_name: remna-agent
    restart: always
    network_mode: host
    environment:
      AGENT_NAME: ${AGENT_NAME}
      AGENT_TOKEN: ${AGENT_TOKEN}

  bot:
    build: ./bot
    container_name: remna-bot
    restart: always
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      AGENT_TOKEN: ${AGENT_TOKEN}
      AGENTS: ${AGENTS}
